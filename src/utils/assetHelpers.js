export const getPartnerLogo = (filename) => {
  return `/images/partners/${filename}`;
};

export const getTrustedByLogo = (filename) => {
  return `/images/trustedby/${filename}`;
};

export const getNavigationIcon = (filename) => {
  return `/images/navigation/${filename}`;
};